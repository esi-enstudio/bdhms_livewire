@props(['title' => '','subtitle' => ''])

<div class="max-w-2xl mx-auto text-center lg:mb-5">
    <h2 class="text-2xl font-bold md:text-4xl md:leading-tight dark:text-white">{{ $title }}</h2>
    <p class="text-gray-600 dark:text-neutral-400">{{ $subtitle }}</p>
</div>
